name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6 
        with:
            go-version-file: 'go.mod'
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Bun dependencies
        run: bun ci

      # Note: templ artifacts are committed; skipping regeneration to avoid generator/runtime mismatches

      - name: Build Tailwind CSS
        run: bunx @tailwindcss/cli -i ./static/css/input.css -o ./static/css/output.css --minify

      - name: Build Go packages
        run: go build ./...

      - name: Build server binary
        run: go build -o verse ./cmd/server

      - name: Start server (background)
        run: |
          nohup ./verse > server.log 2>&1 &
          echo $! > server.pid

      - name: Wait for server
        run: |
          set -euo pipefail
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8080/ >/dev/null 2>&1; then
              echo "server is up"
              exit 0
            fi
            echo "waiting for server... ($i/30)"
            sleep 1
          done
          echo "server did not start within timeout; dumping server.log"
          if [ -f server.log ]; then
            echo "---- server.log (last 200 lines) ----"
            tail -n 200 server.log || true
            echo "-------------------------------------"
          else
            echo "server.log not found"
          fi
          exit 1

      - name: Smoke test GET /
        run: curl -fsS http://localhost:8080/

      - name: Smoke test POST /poem
        run: |
          curl -fsS -X POST -d "content=ci-test" http://localhost:8080/poem | grep -q "Bloom recorded."

      - name: Stop server
        if: always()
        run: |
          kill $(cat server.pid) || true
